{%- comment -%}
  vp-reveal
  Shared scroll-triggered "flashy" reveal animation for VP sections.
  Include once per section: {% render 'vp-reveal' %}

  Mark elements to animate with class "vp-anim".
  Optional stagger: add data-vp-delay="1" (2,3...) to order the reveal,
  or wrap children in a [data-vp-stagger] container to auto-stagger them.
{%- endcomment -%}

{%- comment -%}
  Reveal animation removed by request. The `.vp-anim` / `data-vp-delay` /
  `data-vp-stagger` markup is left in place across the vp- sections, but this
  snippet no longer hides or animates anything — elements render in their final
  visible state. (No-op kept so callers don't need editing; delete the render
  calls later if desired.)
{%- endcomment -%}
